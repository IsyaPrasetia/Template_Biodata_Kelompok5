<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient_background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/splashLogo"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:background="@drawable/bg_circle_logo"
            android:clipToOutline="true"
            android:contentDescription="Logo Kelas"
            android:elevation="8dp"
            android:outlineProvider="background"
            android:scaleType="centerCrop"
            android:src="@drawable/logo_kelas" />

        <TextView
            android:id="@+id/splashTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin_large"
            android:alpha="0.8"
            android:text="Tugas Akhir Kelompok 5"
            android:textColor="@color/text_on_primary"
            android:textSize="@dimen/text_medium"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/splashSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin_small"
            android:alpha="0.6"
            android:text="Mobile Computing"
            android:textColor="@color/text_on_primary"
            android:textSize="@dimen/text_xsmall" />

    </LinearLayout>

</FrameLayout>
